# Figures

| file | shows | produced by |
|---|---|---|
| `two_group.png` | Two-group search results in the `(N, k)` plane, `l=2,3,4` | `searches/two_group.ipynb` |
| `two_group.pdf`, `two_group_ext.png` | Print/extended variants of the above | manually derived from the notebook figure |
| `sequential.png` | Sequential search results in the `(N, k)` plane, `l=2,3,4` | `searches/sequential.ipynb` |
| `block_search_levels.png` | Symmetry-free search results plotted over the two-group catalogue as background | `searches/symfree_search.py` |
| `four_qubit_quirk_circuits.{png,pdf,svg}` | Explicit 4-qubit factory circuits, exported from [Quirk](https://algassert.com/quirk) | external (Quirk), not regenerated by any script here |
| `five_six_qubit_quirk_circuits.{pdf,svg}` | Explicit 5- and 6-qubit factory circuits, exported from Quirk | external (Quirk) |

Regenerate the two auto-generated ones by running their notebook/script (see `searches/README.md`).
Both write here via a `../figures/...` relative path, so run them from inside `searches/`.
